using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace OpenAI
{
    public class OpenAIFunction : MonoBehaviour
    {
        protected OpenAIApi openai = new OpenAIApi("INSERTAPIKEYHERE", null);

        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}
